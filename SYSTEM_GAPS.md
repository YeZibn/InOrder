# 当前系统待完善事项

记录日期：2026-08-24

## P0：正确性与稳定性

- LLM 网关返回 503、超时或流式中断时，CLI 目前可能直接抛出 traceback；需要增加友好的错误提示、重试边界和必要的降级策略。
- 货物画像字段缺失时，不能把未知值直接当成 `0` 参与车型估算，避免错误判定车型可行。
- 货物重量、数量、体积和尺寸仍以原始表达为主，单位换算和多轮累加规则需要统一。
- 增加真实网关、流式调用和 LangExtract 的回归验证。

## P1：工作流架构

- 当前 `full` 链路由 Python Runner 顺序调用意图图和订单图，还不是完整的 LangGraph 父图嵌套子图结构。
- 后续应明确父图、意图子图、问答子图和下单子图之间的状态传递与路由边界。
- 需要统一代码、README 和 OpenSpec，清理已经取消的 clarification 等过期描述。

## P2：会话与订单状态

- 当前 `CliSession` 只保存在进程内，退出 CLI 后历史对话和 `OrderContext` 会丢失。
- 尚未支持多 session 创建、列表、切换和重启恢复。
- 后续可使用 SQLite 持久化 session、conversation turns 和订单上下文快照。

## P3：真实订单业务

- 当前系统只完成意图识别、Rewrite、Extract、上下文更新、货物画像和车型估算。
- 尚未接入历史订单查询、草稿修改、订单创建、确认下单和订单状态流转。
- 后续应通过独立业务 service/API 接入，不将真实业务操作直接放入 LLM resolver。

## 当前定位

目前系统属于“订单语义解析与车型预估原型”，尚未形成完整的拉货下单闭环。
