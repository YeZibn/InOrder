## 1. 画像契约与模型

- [x] 1.1 将货物画像数据模型简化为 `name`、`weight_kg`、`volume_m3`、`dimensions_cm`、运输属性和 `reason`
- [x] 1.2 将汇总模型简化为直接的总重量、总体积和必要解释字段
- [x] 1.3 更新解析校验，删除旧元数据和车辆字段检查之外的过时要求，并校验 `reason` 为非空字符串

## 2. LLM 画像生成

- [x] 2.1 重写货物画像 prompt，要求有可用信息时尽力估算核心数值
- [x] 2.2 明确只有完全无法估算时才允许核心数值为 `null`
- [x] 2.3 要求 `reason` 解释明确值、推导值、估算值和无法估算原因

## 3. 上下文与测试

- [x] 3.1 更新 OrderContext 画像序列化和整体替换逻辑以适配新结构
- [x] 3.2 更新现有货物画像测试和 JSON fixture
- [x] 3.3 增加“有部分信息时必须估算”和“仅货物名称时允许 null”的解析测试
- [x] 3.4 运行货物画像相关测试及项目测试，确认无旧 schema 残留
