## 1. 结果模型与计算基础

- [x] 1.1 扩展车型决策结果，支持单个用户匹配车型和 0 至 3 个估算候选
- [x] 1.2 实现车型表范围上限读取、总重量/总体积快速过滤和特殊规格过滤

## 2. 简化极点装载

- [x] 2.1 将每条货物画像转换为独立整体箱，按体积和最大边排序
- [x] 2.2 实现六种旋转方向、极点生成、边界检查和货物间重叠检查
- [x] 2.3 实现车型候选排序并限制最多三个，不足时不补不可行车型

## 3. 车型解析与图接入

- [x] 3.1 移除车型估算 LLM 调用，接入确定性车型计算器
- [x] 3.2 保留用户车型匹配成功短路逻辑，并将首选估算车型写入上下文、候选保留在结果中
- [x] 3.3 更新订单图状态和 CLI 输出，展示最多三个候选及来源/原因

## 4. 验证

- [x] 4.1 增加多货物不同尺寸、旋转、边界和重叠测试
- [x] 4.2 增加用户车型匹配短路、无车型估算、模糊车型回退和候选数量测试
- [x] 4.3 在 conda `agent` 环境运行完整测试并确认原有车型归一、货物画像和订单图行为兼容
