## 1. 数据模型与边界

- [x] 1.1 定义主意图、订单子意图、澄清状态和 IntentPlan 数据模型。
- [x] 1.2 定义步骤 ID、arguments、depends_on 和原始用户消息字段的序列化结构。
- [x] 1.3 定义识别错误、结构化输出错误和计划校验错误。

## 2. 意图识别子图

- [x] 2.1 实现主意图识别节点，支持 `order`、`qa` 和 `ambiguous`。
- [x] 2.2 实现订单子意图多标签提取与规范化，支持 `create_order`、`modify_draft`、`query_history_order`。
- [x] 2.3 实现顺序依赖和 `depends_on` 关系构建，覆盖历史订单信息引用到草稿修改的场景。
- [x] 2.4 按节点顺序组装意图识别子图：主意图、子意图、规范化、计划构建、计划校验。
- [x] 2.5 通过现有 LLMClient 注入模型调用边界，并确保子图不调用业务工具。

## 3. 计划校验与测试

- [x] 3.1 校验枚举值、步骤 ID 唯一性、依赖存在性、顺序合法性和依赖无环。
- [x] 3.2 处理低置信度、缺失参数和无法消解歧义，输出 needs_clarification 及原因。
- [x] 3.3 编写单主意图、单子意图、多子意图和顺序依赖的 mock 测试。
- [x] 3.4 编写非法输出、缺失信息、无效依赖和业务副作用隔离测试。
- [x] 3.5 在 conda `agent` 环境运行完整测试和静态检查。

## 4. 集成说明

- [x] 4.1 提供主图调用意图识别子图并消费 IntentPlan 的示例。
- [x] 4.2 明确本阶段不实现订单查询、草稿修改、订单创建、确认下单或问答回答。
