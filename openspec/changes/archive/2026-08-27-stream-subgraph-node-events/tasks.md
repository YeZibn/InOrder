## 1. 后端子图事件适配

- [x] 1.1 扩展 `WorkflowEventAdapter` 以开启并解析 LangGraph `subgraphs=True` 的 v1/v2 updates 流。
- [x] 1.2 建立意图、订单、画像、车型及订单检查节点的安全阶段/标题白名单，并为节点完成事件生成递增序号。
- [x] 1.3 对父图聚合更新、未知节点和重复节点进行过滤，保持现有 `CREATE_ORDER_CONTEXT`、`DONE`、`ERROR` 生命周期及结果结构兼容。

## 2. 前端实时展示

- [x] 2.1 调整 SSE 事件处理逻辑，按节点完成事件顺序追加进度记录，即使公开阶段相同也不覆盖既有提示。
- [x] 2.2 为新增节点事件补充用户友好的文案映射，并确保不渲染内部节点名、原始响应或完整事件 JSON。
- [x] 2.3 保持终态摘要、上下文、history、错误和断开请求行为不变。

## 3. 测试与验证

- [x] 3.1 增加 v1/v2 嵌套子图 updates 的适配器测试，验证节点事件顺序、序号、去重及可选节点缺失行为。
- [x] 3.2 增加 QA、错误和终态兼容测试，确认不产生订单节点或伪造成功结果。
- [x] 3.3 增加前端静态映射/重复阶段追加测试，并运行相关 SSE、工作流和 API 测试。
