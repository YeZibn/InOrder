## 1. 车型决策模型

- [x] 1.1 定义可序列化车型决策结果，包含最终车型、特殊规格、source 和 reason
- [x] 1.2 增加从 `vehicles.json` 校验 canonical vehicle code 的能力接口

## 2. 车型匹配与估算

- [x] 2.1 实现用户车型优先逻辑：精确匹配或严格 Ensemble 成功时直接采用并跳过估算
- [x] 2.2 实现无车型/匹配失败时基于完整货物画像和车型能力字段的车型估算 resolver
- [x] 2.3 校验估算输出只能使用主数据中的车型 code，并保留无法匹配的原文和原因

## 3. LangGraph 与 CLI 接入

- [x] 3.1 新增车型解析节点并接入 CargoProfile 之后的订单子图状态流
- [x] 3.2 扩展订单图结果和 CLI 输出，展示最终车型、规格、来源和原因

## 4. 验证

- [x] 4.1 增加用户车型匹配成功、用户未提供车型和模糊车型回退估算测试
- [x] 4.2 增加用户匹配车型不被估算覆盖、非法估算 code 被拒绝的测试
- [x] 4.3 在 conda `agent` 环境运行完整测试并确认现有归一化、货物画像和订单图行为兼容
