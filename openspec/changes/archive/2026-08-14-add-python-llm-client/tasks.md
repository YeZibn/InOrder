## 1. Python 项目基础

- [x] 1.1 建立 Python 包结构、开发依赖和最小运行配置。
- [x] 1.2 定义环境变量名称、默认值、类型转换和缺失配置校验。

## 2. LLM 客户端核心

- [x] 2.1 定义消息输入、统一响应对象和 usage 数据结构。
- [x] 2.2 定义配置错误、参数错误、认证错误、限流错误、超时错误和通用上游错误。
- [x] 2.3 实现 OpenAI-compatible 客户端适配，完成消息映射和响应归一化。
- [x] 2.4 实现有限重试、退避和限流等待处理，并确保不重试认证及参数错误。
- [x] 2.5 提供可注入的传输或 SDK 边界，使客户端可以被 mock。
- [x] 2.6 在配置对象中加入可选 reasoning effort，并校验值只能为 `low`、`medium` 或 `high`。
- [x] 2.7 在已配置 reasoning effort 时将 `reasoning_effort` 透传到 Chat Completions 请求，未配置时省略该字段。

## 3. 验证入口与测试

- [x] 3.1 实现最小 CLI/脚本入口，读取配置、发送示例消息并输出非敏感结果。
- [x] 3.2 编写配置解析、空消息、成功响应和 usage 归一化测试。
- [x] 3.3 编写认证、超时、限流、通用上游错误及重试行为测试。
- [x] 3.4 运行完整测试和本地验证入口，确认配置缺失时非零退出且不泄露密钥。
- [x] 3.5 增加 reasoning effort 配置、非法值和请求透传/省略行为测试。

## 4. LangGraph 预留边界

- [x] 4.1 提供一个不依赖 LangGraph 实现的客户端调用示例或适配说明，明确后续节点可直接注入该客户端。
- [x] 4.2 验证本阶段未引入拉货业务状态、Graph checkpoint、流式输出或工具调用。
