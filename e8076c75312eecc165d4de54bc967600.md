# AI 下单 Agent v2 技术设计文档

> 来源：`e8076c75312eecc165d4de54bc967600.mp4` 画面 OCR 提取
>
> 视频时长：约 99.5 秒
>
> 说明：以下内容根据视频中滚动显示的设计文档整理。由于原视频为拍摄电脑屏幕，部分字段名、代码和数字可能存在 OCR 误识别，建议以项目源文件为准。

## 目录

1. 整体架构
2. 意图识别模块
3. 信息抽取模块
4. 车型归一化模块
5. 货物推断模块
6. 车型推荐模块
7. 结果构建
8. SSE 流式事件推送
9. 多货物场景说明
10. State 状态
11. 配置与变更

## 1. 整体架构

### 1.1 系统定位

AI 下单 Agent v2 面向拉货下单场景，负责从用户自然语言中识别意图、抽取订单信息、归一化车型、推断货物属性、推荐车型，并通过 SSE 将处理过程和订单上下文推送给前端。

整体调用关系：

```text
前端 / Java 服务
    │ WebClient POST /api/v2/chat
    ▼
Python Agent（bfe-customer-ai-agent-svc）
    ├─ 意图识别：下单 / 知识问答
    ├─ 信息抽取：自然语言 → 结构化订单信息
    ├─ 车型归一化：口语化车型名 → 标准车型
    ├─ 货物推断：品类归一、单位换算、缺失值估算、运输约束推断
    ├─ 车型推荐：根据货物信息推荐车型
    └─ 结果构建：生成 order_context 快照并通过 SSE 推送
```

### 1.2 LangGraph 主子图编排

V2 采用 LangGraph 主图与子图分层架构，共享 `MasterState`：

```text
START
  ↓
intent（意图识别子图）
  ↓
route_after_intent
  ├─ knowledge_qa
  └─ create_order → info_extract
                         ↓
                    vehicle_normalize
                         ↓
                    check_cargo
                         ↓
                    cargo_infer
                         ↓
                    vehicle_picker
                         ↓
                    build_result
```

视频中展示的相关模块/文件包括：

- `vehicle_normalize.py`
- `cargo_infer.py`
- `vehicle_picker.py`
- `build_result.py`
- `info_extract/`
  - `rewrite.py`
  - `extraction.py`
  - `entities.py`
  - `post_process.py`
  - `vehicle_match.py`
  - `thinking_steps.py`
- `infrastructure/llm/`
  - `config.py`
  - `config.toml`
  - `logger.py`

## 2. 意图识别模块

### 2.1 模块定位

意图识别是 AI 下单系统的第一道网关。V2 将 V1 的单次 LLM 调用拆分为两步独立调用：

1. 主意图识别：判断用户属于 `create_order` 还是 `knowledge_qa`。
2. 子意图识别：仅当主意图为 `create_order` 时调用，进一步分流为：
   - `extract_order_info`
   - `confirm_order`
   - `reorder`

拆分收益：

- 每个 Prompt 更聚焦、更短，提升模型分类准确率。
- `knowledge_qa` 场景无需执行子意图调用，减少一次 LLM 调用。
- 便于独立迭代和 A/B 测试。

### 2.2 输出格式

要求模型输出一个 JSON 对象，示意字段如下：

```json
{
  "intent": "create_order",
  "confidence": 0.98,
  "reason": "用户表达了明确的下单需求"
}
```

主意图示例：

| 用户输入 | 识别结果 |
|---|---|
| 从上海到北京拉一吨苹果 | `create_order` |
| 物流平台规则怎么收费 | `knowledge_qa` |
| 修改装货地址为上海虹桥 | `create_order` |
| 明天下午三点能不能拉 | `create_order` |

子意图示例：

| 用户输入 | 子意图 |
|---|---|
| 从 A 地区拉冰箱去 B 地区 | `extract_order_info` |
| 确认下单 | `confirm_order` |
| 新时间改为明天，然后确认 | `confirm_order` |
| 按历史订单再来一单 | `reorder` |

调用约束：

```text
workflow_id: AI_ORDER_MAIN_INTENT_WORKFLOW_ID
response_format: {"type": "json_object"}
```

## 3. 信息抽取模块

### 3.1 模块定位

信息抽取模块是下单流程的核心，负责从用户对话和订单上下文中提取结构化订单信息。

流程示意：

```text
用户输入
  ↓
上下文重写（必要时）
  ↓
LangExtract 结构化抽取
  ↓
实体收集与后处理
  ↓
OrderEntities
```

视频中显示的调用参数包括：

```text
workflow_id: AI_ORDER_INFO_EXTRACT_V2_WORKFLOW_ID
temperature: 0
```

### 3.2 实体字段

抽取结果覆盖以下信息：

- 车型：`vehicle_type`
- 车型规格：`vehicle_specs`
- 货物名称、重量、尺寸、体积、数量
- 多货物列表：`cargo_items`
- 服务类型：`service_type`
- 装货时间：`pickup_time`
- 装货地点、卸货地点
- 装货城市、卸货城市
- 发货人姓名、手机号
- 收货人姓名、手机号
- 备注片段：`remark_fragments`
- 跟车人数：`follow_car_number`
- 是否本人跟车：`oneself_follow_flag`
- 发票类型：`invoice_type`
- 支付方式：`payment_type`

### 3.3 实体收集器

`Entities.py` 的设计目标是将 LangExtract 的结果按上下文分流为历史订单条件和新订单信息，并支持多货物收集：

```python
@dataclass
class OrderEntities:
    history_search: dict  # 历史订单查询条件
    new_order: dict       # 新订单信息
```

`new_order` 可能包含：

```text
vehicle_type, vehicle_specs
cargo_name, cargo_weight, cargo_dimensions
cargo_volume, cargo_quantity, cargo_items
service_type
pickup_time, pickup_location, dropoff_location
pickup_city, dropoff_city
sender_surname, sender_name, sender_phone
receiver_surname, receiver_name, receiver_phone
remark_fragments
follow_car_number, oneself_follow_flag
invoice_type, payment_type
```

## 4. 车型归一化模块

车型归一化负责将用户口语化、模糊或带规格描述的车型转换为标准车型。视频中展示的归一化与校验流程包括：

```text
info_extract
  ↓
vehicle_normalize
  ↓
check_cargo
```

车型字段约束：

- `vehicle_type`：标准车型名称。
- `vehicle_specs`：车型规格修饰词，例如“高顶”“封闭式”“带尾板”等；每个修饰词单独记录。
- 对“X 米以上”的表达，映射为不小于 X 且最接近 X 的标准车长。

## 5. 货物推断模块

### 5.1 模块定位

货物推断模块将信息抽取阶段得到的原始货物信息转换为结构化数据，主要负责：

- 单位归一化：例如 `kg → t`、`cm → m`、立方单位归一化。
- 缺失数值估算：当重量等字段为空时，根据品类常识估算。
- 多货物支持：输出 `cargo_items` 数组及聚合字段。
- 运输约束传递：将 `state.remark` 中的“需要冷藏”“易碎”等约束传给 LLM，生成 `needs_cold`、`fragile` 等属性。

### 5.2 输入示例

```text
货物 1：苹果，重量 1 吨，数量 1
货物 2：香蕉，重量 508 kg，数量 2
运输要求：需要冷藏
```

备注仅在非空时追加，用于表达用户明确提出的运输约束。

### 5.3 输出格式

```json
{
  "cargo_items": [
    {
      "category": "苹果",
      "weight_t": 1.0,
      "volume_m3": 0.0,
      "max_length_m": 0.0,
      "quantity": 1,
      "total_weight_t": 1.0,
      "total_volume_m3": 0.0,
      "stackable": false,
      "fragile": false,
      "needs_cold": false,
      "reason": "用户明确提供重量 1 吨，直接转换"
    }
  ],
  "cargo_total_weight_t": 2.0,
  "cargo_total_volume_m3": 0.0,
  "cargo_max_length_m": 0.0,
  "cargo_needs_cold": false
}
```

## 6. 车型推荐模块

### 6.1 车型库

视频中显示标准车型共约 33 种，分为 5 大类：

1. 面包车系列
2. 货车系列（中小型）
3. 中大型货车
4. 大型货车
5. 冷运系列

另有特殊车型，例如跑腿、四轮小件等。

车型示例：

- 面包车：微面、小面、中面、大面、依维柯
- 小型货车：小厢货、小平板、小栏货、中厢货、中平板、中厢货
- 中大型货车：3 米 8、4 米 2、5 米 2、6 米 2、6 米 8、7 米 6、8 米 2、8 米 6、9 米 6
- 大型货车：11 米 7、12 米 6、13 米、13 米 7、16 米、17 米 5
- 冷运系列：冷运 3 米 8、冷运 4 米 2、冷运 5 米 2、冷运 6 米 8、冷运 7 米 6、冷运 9 米 6

### 6.2 推荐结果

推荐结果包含最佳、较大和较小候选车型：

```json
{
  "recommend_vehicle": {
    "best": {"vehicle_id": 101},
    "larger": {"vehicle_id": 103},
    "smaller": {"vehicle_id": 100},
    "reason": "根据货物重量、体积、长度及运输约束推荐"
  }
}
```

## 7. 结果构建

`build_result` 节点负责汇总前置节点结果，生成 `order_context` 快照，供前端展示以及后续确认下单使用。

示例字段：

```json
{
  "loading_address": "上海科技园",
  "unloading_address": "温州",
  "loading_city": "上海",
  "unloading_city": "温州",
  "pickup_time": "2026-08-06 15:00",
  "pickup_timestamp": 1754467200,
  "cargo_name": "苹果",
  "cargo_weight": "1 吨",
  "cargo_items": [
    {"category": "苹果", "total_weight_t": 1.0}
  ],
  "recommend_vehicle": {
    "best": {"vehicle_id": 101},
    "larger": {"vehicle_id": 103},
    "smaller": {"vehicle_id": 100}
  },
  "sender_name": "张师傅",
  "sender_phone": "13800138088",
  "receiver_name": "辛师傅",
  "receiver_phone": "13988139680",
  "payment_type": 0,
  "follow_car_number": 2,
  "remark": "装货地电话联系"
}
```

## 8. SSE 流式事件推送

### 8.1 事件格式

每条消息以 `data:` 开头，以两个换行结束，符合 SSE 协议标准：

```text
data: {"type":"THINKING_START", "payload": {...}}

```

### 8.2 事件类型

视频中展示的事件包括：

- `THINKING_START`：开始思考
- `THINKING_STEP`：思考步骤，包含 `step_index`、`title`、`content`、`intent` 等字段
- `THINKING_DONE`：思考完成
- `CREATE_ORDER_CONTEXT`：推送订单上下文
- `DONE`：流式响应结束

### 8.3 API 示例

```python
@router.post("/api/v2/chat")
async def chat_v2(body: ChatRequest) -> StreamingResponse:
    async def gen() -> AsyncIterator[str]:
        graph = get_master_graph()
        initial_state = {
            "message": body.message,
            "conversation_history": body.conversation_history,
            "previous_order_context": body.previous_order_context,
        }

        request_id = str(uuid.uuid4())
        config = {
            "configurable": {"thread_id": f"req_{request_id}"}
        }

        async for event in graph.astream_events(
            initial_state,
            version="v2",
            config=config,
        ):
            if event.get("event") == "on_custom_event":
                custom_data = event.get("data")
                if custom_data:
                    yield format_sse(custom_data)

        yield format_sse(SSEEvents.done())

    return StreamingResponse(
        gen(),
        media_type="text/event-stream",
    )
```

### 8.4 事件发布工具

事件发布通过 LangGraph 的 `dispatch_custom_event` 和 `get_stream_writer` 完成。项目中嵌套子图的事件需要统一转发到主图流中，以便前端持续接收思考过程和订单结果。

## 9. 多货物场景说明

示例用户输入：

```text
再加一箱香蕉
```

处理过程：

1. 重写模块结合订单上下文理解“再加一箱香蕉”。
2. LangExtract 抽取出新增货物实体。
3. `Entities` 收集多个货物，形成 `cargo_items` 列表。
4. `slot_merger` 合并新旧货物槽位。
5. `cargo_infer` 对全部货物进行品类归一、单位换算和聚合计算。

示意：

```text
state.cargo_items = [{name: "苹果", ...}]
        + [{name: "香蕉", ...}]
        ↓
state.cargo_items = [
  {name: "苹果", ...},
  {name: "香蕉", ...}
]
        ↓
cargo_infer
        ↓
cargo_items = [
  {category: "苹果", ...},
  {category: "香蕉", ...}
]
```

## 10. State 状态

### 10.1 `OrderCreatorState`

视频中展示的主要字段如下：

```python
class OrderCreatorState(TypedDict, total=False):
    session_id: str
    message: str
    conversation_history: list[dict[str, str]]
    previous_order_context: dict
    sender_name: str
    sender_phone: str
    receiver_name: str
    receiver_phone: str
    reorder_order_id: str
    reorder_search_condition: dict
    current_stage: str
    user_abort_flag: bool
    context_summary: str
    order_context: dict
    thinking_steps: list[dict]
```

### 10.2 `MasterState`

`MasterState` 是 `OrderCreatorState` 的超集，额外包含意图识别字段：

```python
class MasterState(OrderCreatorState, total=False):
    intent: str
    confidence: float
    reason: str
```

## 11. 完整时序示例

用户输入：

```text
从上海到北京，拉一吨苹果
```

示意时间线：

```text
0.00s  POST /api/v2/chat
0.15s  THINKING_START
0.50s  THINKING_STEP 1：分析用户意图与车型/货物信息
1.00s  THINKING_STEP 2：识别装卸城市和货物
2.05s  THINKING_STEP 3：确认上海到北京、苹果一吨
2.55s  THINKING_STEP 4：推算合适车型
3.05s  THINKING_DONE
5.05s  CREATE_ORDER_CONTEXT {order_context: ...}
       DONE
```

## 12. 版本历史

视频末尾显示的版本历史包括：

| 版本 | 主要变更 |
|---|---|
| V2.0 | 初始版本：主子图编排、两步意图识别、LangExtract 信息抽取 |
| V2.1 | 新增跨轮状态管理，透传 `previous_order_context` |
| V2.2 | 车型库扩展，新增冷运系列规格 |
| V2.3 | 冷链相关输入不默认映射，未匹配时返回 `null` |
| V2.4 | 新增 LangGraph Checkpointer，支持进程内 `MemorySaver` 持久化 |
| V2.5 | 新增 LangGraph `RetryPolicy`，支持节点级自动重试 |
| V2.6 | 新增 Layer 2 货物归一化节点 `cargo_normalize` |
| V2.7 | 实现 `vehicle_picker` 节点 |
| V2.8 | 实现 `build_result` 节点 |
| V2.9 | 补充 State 字段 |

---

*以上内容为视频画面 OCR 后的整理版；原始录屏中的代码和字段请以实际项目文件为准。*
